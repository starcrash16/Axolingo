<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/grad_menuprincipal">

    <!-- 1. Imagen de Perfil -->
    <ImageView
        android:id="@+id/imgProfile"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginTop="64dp"
        android:src="@android:drawable/ic_menu_manage"
        android:background="@drawable/white_circle_background"
        android:padding="4dp"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:contentDescription="Foto de perfil" />

    <!-- 2. Nombre de Usuario -->
    <TextView
        android:id="@+id/tvUsername"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nombre de Usuario: Nombre de usuario"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/white"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/imgProfile"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 3. Botón Cambiar Contraseña -->
    <Button
        android:id="@+id/btnChangePassword"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cambiar Contraseña"
        android:backgroundTint="@color/white"
        android:textColor="#0960fb"
        android:layout_marginTop="32dp"
        app:layout_constraintTop_toBottomOf="@+id/tvUsername"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- 4. Tabla de Puntuaciones -->
    <TableLayout
        android:id="@+id/scoreTable"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:layout_marginHorizontal="16dp"
        android:background="@drawable/rounded_card_background"
        android:padding="8dp"
        android:stretchColumns="1"
        app:layout_constraintTop_toBottomOf="@+id/btnChangePassword"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TableRow android:padding="4dp">
            <TextView android:text="Nivel" android:textStyle="bold" android:textColor="#000" android:paddingEnd="8dp"/>
            <TextView android:text="Puntuación" android:textStyle="bold" android:textColor="#000" android:gravity="end" android:paddingEnd="48dp"/>
        </TableRow>
        <View android:layout_height="1dp" android:background="#CCC" android:layout_marginBottom="4dp"/>

        <TableRow android:padding="4dp">
            <TextView android:text="Spelling Bee" android:textColor="#333"/>
            <TextView android:id="@+id/tvScoreSpell" android:text="0.0" android:textColor="#333" android:gravity="end" android:paddingEnd="48dp"/>
        </TableRow>

        <TableRow android:padding="4dp">
            <TextView android:text="Reading Comprehension" android:textColor="#333"/>
            <TextView android:id="@+id/tvScoreReading" android:text="0.0" android:textColor="#333" android:gravity="end" android:paddingEnd="48dp"/>
        </TableRow>

        <TableRow android:padding="4dp">
            <TextView android:text="Vocabulary" android:textColor="#333"/>
            <TextView android:id="@+id/tvScoreVocab" android:text="0.0" android:textColor="#333" android:gravity="end" android:paddingEnd="48dp"/>
        </TableRow>

        <TableRow android:padding="4dp">
            <TextView android:text="Translator" android:textColor="#333"/>
            <TextView android:id="@+id/tvScoreTransl" android:text="0.0" android:textColor="#333" android:gravity="end" android:paddingEnd="48dp"/>
        </TableRow>

        <TableRow android:padding="4dp">
            <TextView android:text="Pizarra Mágica" android:textColor="#333"/>
            <TextView android:id="@+id/tvScoreBoard" android:text="0.0" android:textColor="#333" android:gravity="end" android:paddingEnd="48dp"/>
        </TableRow>

        <TableRow android:padding="4dp">
            <TextView android:text="Canasta" android:textColor="#333"/>
            <TextView android:id="@+id/tvScoreShapes" android:text="0.0" android:textColor="#333" android:gravity="end" android:paddingEnd="48dp"/>
        </TableRow>
    </TableLayout>

    <!-- ================= FOOTER ================= -->

    <!-- Contenedor del Footer -->
    <LinearLayout
        android:id="@+id/bottomNavContainer"
        android:layout_width="0dp"
        android:layout_height="80dp"
        android:orientation="horizontal"
        android:background="@drawable/footer"
        android:elevation="8dp"
        android:paddingTop="25dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Botón Home -->
        <ImageButton
            android:id="@+id/btnHome"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:src="@drawable/icono_home"
            android:background="@android:color/transparent"
            android:scaleType="centerInside"
            android:padding="12dp"
            app:tint="@android:color/darker_gray"
            android:contentDescription="Home"/>

        <!-- Espacio central -->
        <View
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"/>

        <!-- Botón Settings (Activo o desactivado ya que estamos aquí) -->
        <ImageButton
            android:id="@+id/btnSettings"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:src="@drawable/icono_settings"
            android:background="@android:color/transparent"
            android:scaleType="centerInside"
            android:padding="12dp"
            app:tint="#0960fb"
            android:contentDescription="Settings"/>
    </LinearLayout>

    <!-- TextView para el punchline (oculto inicialmente) -->
    <TextView
        android:id="@+id/tv_punchline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:textSize="1sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Botón Flotante Central (Estilo Axolote) -->
    <ImageButton
        android:id="@+id/btn_chiste"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:background="@android:color/transparent"
        android:padding="0dp"
        android:src="@drawable/axo_rosa_boton"
        app:tint="@null"
        android:contentDescription="Acción principal"
        android:elevation="12dp"
        android:layout_marginBottom="30dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>